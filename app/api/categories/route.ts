import { NextResponse } from 'next/server';

// Placeholder route to satisfy TypeScript build checks
export async function GET() {
  return NextResponse.json(
    { error: 'Not implemented' },
    { status: 501 }
  );
}

export async function POST() {
  return NextResponse.json(
    { error: 'Not implemented' },
    { status: 501 }
  );
}